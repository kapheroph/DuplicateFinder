# Application icon

Place the Windows icon here as:

`bills_duplicate_finder.ico`

The Windows build script embeds this icon into the executable and also bundles it for the running application window.
